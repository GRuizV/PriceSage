# Copy to .env (gitignored) and fill in. The collector loads .env automatically.

# Neon Postgres connection string (Dashboard -> Connect -> psql/URI).
# Keep the ?sslmode=require that Neon includes.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/dbname?sslmode=require
